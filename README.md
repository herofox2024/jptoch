﻿﻿﻿﻿﻿﻿# EPUB 日译中翻译工具

一个基于大模型 API 的日文 EPUB 自动翻译工具，提供桌面 GUI，支持将日文 EPUB 翻译为简体中文，并尽量保留原书结构（目录、链接、章节）。

## 新增能力（v2.0）

- **UI 全面重构**：从单页表单改为"左侧导航 + 右侧多页面"布局
  - 左侧深色导航栏，4 个页面切换：任务 / 接口配置 / 翻译选项 / 状态监控
  - 卡片化组件（LabelFrame 包裹各功能模块）
  - 主题配色系统（`THEME` 字典统一管理颜色）
  - 标题显示版本号"EPUB 翻译器 V2.0.1"
- **接口配置改为下拉框**：服务提供方从 Radiobutton 改为 Combobox 下拉选择
- **状态监控页增强**：
  - 8 个统计卡片：已完成、总文本块、新增术语、耗时、速度、API 请求、成功率、失败数
  - 翻译进度条
  - 实时翻译展示：逐条显示当前翻译的日文原文与中文译文
- **翻译状态输出优化**：进度条下方的详细状态信息改为输出到日志文件，UI 仅保留卡片和实时翻译展示
- **自动页面切换**：点击"开始翻译"后自动跳转到状态监控页
- **translator.py 回调增强**：`translate_batch` 新增 `item_callback` 参数，每条翻译完成时回调原文与译文
- **打包支持**：支持 PyInstaller 打包为单文件 `EPUB日译中V2.0.exe`

## 功能概览

- EPUB 正文翻译（段落、标题、列表、引用等常见标签）
- 目录（NCX）标题翻译
- 超链接结构保留（保留 `href`，替换文本）
- Ruby 注音处理（提取正文语义，去除注音标签干扰）
- 图片占位文本过滤（避免误翻）
- 翻页方向设置（中文习惯 / 保持原版）
- 缓存机制（减少重复翻译）
- 术语表支持（`glossary.json`）
- 多线程批量翻译
- 性能预设模式（默认/适中/极端）

## 新增能力（v1.5_a）

- **性能预设功能**：新增三种预设模式，一键优化翻译速度
  - `默认`：稳定安全，适合所有账户（并发5，批量4）
  - `适中`：推荐配置，效率与稳定性兼顾（并发12，批量10），预估提速 40-60%
  - `极端`：极限速度，高风险模式（并发25，批量15），会弹出风险警告
- **代码质量提升**：
  - 修复 `epub_io.py` 未定义变量和文件编码损坏问题
  - 细化异常处理，捕获具体异常类型
  - 修复线程安全问题，使用 `threading.Event` 替代布尔变量
  - 添加 API 响应格式验证，避免 KeyError/IndexError
  - 提取魔法数字为类常量，提高可维护性
- **连接池优化**：连接池大小与并发数匹配，消除 "Connection pool is full" 警告
- **UI 改进**：翻译完成时状态显示 "翻译完成" 而非 "翻译中..."

## 新增能力（v1.5）

- 新增服务提供方选择：`DeepSeek` / `Sakura` / `Gemini` / `Custom`
- 新增可编辑配置：`Base URL`、`Model`
- 输入 EPUB 后自动预估可翻译字符数（异步，不阻塞 UI）
- 新增“导入术语表 JSON”按钮（覆盖写入前自动备份）
- 新增“自动提取术语（实验）”开关
- 注意：开启自动术语提取会显著增加 token 消耗，请谨慎使用
- 批量结构化返回升级为 JSON 对象：`translations + new_terms`
- 启用术语提取时：自动清洗、去重并增量写入 `glossary.json`
- 增强 JSON 解析容错（兼容代码块包裹和前后噪声）
- 增强错误处理：对 HTTP 502 快速中断
- 状态统计增强：成功率、JSON 成功率、回退率、JSON 失败计数、API 请求数、新增术语数
- 新增文件日志：`~/.epub_translator/logs/app-YYYYMMDD.log`

## 环境要求

- Python 3.8+
- tkinter（通常随 Python 安装）
- 依赖见 `requirements.txt`

## 安装

```bash
pip install -r requirements.txt
```

## 使用方法

### 启动 GUI（Qt 主线）

```bash
python main_qt.py
```

Tk 兼容入口（冻结维护，仅用于兼容测试）：

```bash
python app.py
```

### V2.0 界面布局

```
┌──────────────────────────────────────────────────────┐
│ ┌──────────┐  ┌───────────────────────────────────── │
│ │EPUB翻译器 │  │  [任务] 页面                         │
│ │  V2.0    │  │   文件设置卡片                       │
│ ├──────────┤  │     输入 EPUB / 输出 EPUB            │
│ │ 任务     │  │   操作卡片                           │
│ │ 接口配置 │  │     [▶ 开始翻译] [取消]              │
│ │ 翻译选项 │  ├───────────────────────────────────── │
│ │ 状态监控 │  │  [接口配置] 页面                     │
│ └──────────┘  │   API配置卡片                       │
│               │     服务提供方/API Key/测试连接/Base URL/模型 │
│  左侧导航     ├───────────────────────────────────── │
│  (点击切换)   │  [翻译选项] 页面                     │
│               │   性能预设 / 翻页方向 / 术语表       │
│               ├───────────────────────────────────── │
│               │  [状态监控] 页面                     │
│               │   8项统计卡片 + 进度条 + 实时翻译    │
│               └───────────────────────────────────── │
└──────────────────────────────────────────────────────┘
```

### 基本步骤

1. 选择输入 EPUB（支持拖拽文件到输入框）
2. 确认输出路径
3. 点击左侧导航栏切换到「接口配置」页面
4. 从下拉框选择服务提供方（DeepSeek / Doubao / Sakura / Gemini / 自定义）
5. 设置 API Key（Sakura 可留空，会使用 `sk-local`）
6. 检查 `Base URL` 与 `Model`（切换服务提供方会自动填充默认值）
7. 点击左侧导航栏切换到「翻译选项」页面
8. 选择性能预设（默认 / 适中 / 极端）
9. 选择翻页方向（中文习惯 / 保持原版）
10. 可选：导入术语表 JSON、开启术语提取或启用术语表
11. 点击左侧导航栏回到「任务」页面
12. 点击「▶ 开始翻译」按钮
13. 界面自动跳转到「状态监控」页面，实时查看：
    - 统计卡片：已完成、总文本块、新增术语、耗时、速度、API请求、成功率、失败数
    - 进度条
    - 实时翻译：逐条显示日文原文与中文译文

## 性能预设说明

| 预设 | 并发数 | 批量大小 | 批量总字符 | 单文本上限 | 分块大小 | 预估提速 |
|------|--------|----------|-----------|-----------|---------|---------|
| 默认 | 5 | 4 | 800 | 200 | 1200 | 基准 |
| 适中 | 12 | 10 | 4000 | 600 | 2500 | +40~60% |
| 极端 | 25 | 15 | 8000 | 1000 | 4000 | +100%+ |

- **默认**：稳定安全，适合免费账户或首次使用
- **适中**：推荐配置，效率与稳定性兼顾
- **极端**：高风险模式，仅建议付费账户、高配电脑、稳定网络使用
  - 可能频繁触发 API 限流（429 错误）
  - 大批量失败回退代价高
  - 内存占用增加

## API Key 与后端说明

- `DeepSeek`：
  - 默认 URL：`https://api.deepseek.com/chat/completions`
  - 默认模型：`deepseek-v4-flash`
  - 需要 API Key
- `Doubao`（火山引擎豆包）：
  - 默认 URL：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`
  - 默认模型：`Doubao-Seed-1.6-flash`
  - 需要 API Key
- `Sakura`（本地/自建 OpenAI 兼容服务）：
  - 默认 URL：`http://127.0.0.1:8080/v1/chat/completions`
  - 默认模型：`sakura-v1.0`
  - API Key 可留空（程序会使用 `sk-local`）
- `Gemini`（OpenAI 兼容入口）：
  - 默认 URL：`https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`
  - 默认模型：`gemini-2.5-flash`
  - 需要 API Key
- `Custom`：
  - 需自行填写可用的 `Base URL`、`Model`、API Key

## 环境变量

可选：预先设置 DeepSeek 或 Doubao Key，减少手动输入。

```bash
# Windows
set DEEPSEEK_API_KEY=your-api-key
set DOUBAO_API_KEY=your-api-key

# Linux/macOS
export DEEPSEEK_API_KEY=your-api-key
export DOUBAO_API_KEY=your-api-key
```

## 术语表

默认路径：`~/.epub_translator/glossary.json`

示例：

```json
{
  "勇者": "勇者",
  "魔王": "魔王",
  "魔法少女": "魔法少女"
}
```

## 缓存与日志

- 缓存：`~/.epub_translator/cache.json`
- 日志：`~/.epub_translator/logs/app-YYYYMMDD.log`

## 项目结构

- `main_qt.py`：Qt 主入口（当前主线）
- `ui/qt_app.py`：Qt 界面与工作线程逻辑
- `app.py`：Tk 兼容入口（FROZEN/legacy，仅必要修复）
- `translator.py`：翻译核心逻辑（API 调用、批处理、缓存、术语处理）
- `epub_io.py`：EPUB 读写、目录与翻页设置
- `cache_store.py`：缓存/JSON 读写（原子写入）
- `text_utils.py`：文本可翻译性判断等公共工具
- `requirements.txt`：依赖清单
- `experimental/qml_v4/`：PySide6 + QML V4 实验版目录，当前不作为稳定发布入口

## 注意事项

- 大文件翻译耗时较长，请耐心等待
- 建议翻译后在阅读器中检查排版与章节跳转
- 若使用自建/代理网关，请确认接口兼容 `chat/completions`
- 免费大模型额度和限流较严格，参数需要保守：
  - 智谱 GLM 免费版：建议并发 `1`、批量 `2-3`、单文本字数约 `200`、批量总字数约 `200`、超时响应 `300` 秒。
  - Gemini 免费版：即使使用与智谱 GLM 免费版相同的保守参数，也可能触发 API 限流。
  - 长篇 EPUB 建议尽量使用付费大模型，例如 DeepSeek，通常翻译速度更快、质量更稳定。

## 版本记录

### QML/V4 实验版（2026-06-11）

- **新增 iOS26 玻璃主题**：
  - QML/V4 设置页的主题下拉框新增 `iOS26 玻璃`，配置值保存为 `glass`，可与浅色纸感、深色墨色主题并存。
  - `AppPalette.qml` 增加玻璃主题变量，统一半透明卡片、玻璃边框、高光、柔光背景、圆角和强调色。
  - 主窗口背景新增青绿色/琥珀柔光光斑，顶部栏和左侧导航改为玻璃态视觉；左侧导航保留胶囊按钮、当前页面激活态和悬停高亮。
  - 玻璃效果采用半透明、渐变、高光边框模拟，不默认启用实时模糊，降低 Windows 桌面环境下的性能风险。
- **状态进度口径修复**：
  - 修复 QML/V4 状态页完成后百分比为 `100%`，但“已翻译 / 总字数”和“文本块”仍停留在最后一次批次回调的问题。
  - 修复批量翻译去重后进度只按去重文本计数的问题，重复文本会按原始出现次数推进完成数。
- **状态页与 API 页布局修复**：
  - 修复 API 配置页表单卡片固定高度过小，导致 `超时(秒)` 和 `测试连接` 按钮溢出背景框的问题；表单高度改为按内部布局自适应。
  - 修复状态监控“实时翻译”页空状态下同时显示标题 `原文/译文` 和输入框占位符 `原文.../译文...`，造成看起来重复的问题；现只保留面板标题。
  - 修复状态监控“校对详情”顶部说明文字超过两行后溢出背景卡片的问题；质检报告头部和报告字段均改为按多行文本高度自适应。
- **安装包打包**：
  - 重新构建最新 QML/V4 绿色版 `dist/EPUB日译中V4.0.exe`。
  - 新增 Inno Setup 脚本 `installer/EPUB日译中V4.0.iss`，生成安装包 `dist/installer/EPUB日译中V4.0安装程序.exe`。
  - 安装包默认安装到 `%LocalAppData%\Programs\EPUB日译中 V4.0`，支持开始菜单快捷方式、可选桌面快捷方式和卸载入口。
  - 当前本机 Inno Setup 未包含 `ChineseSimplified.isl`，安装向导使用默认英文界面；应用名、安装目录、快捷方式和安装包文件名仍保留中文。

### V3.2.1（2026-06-04）

#### Qt UI 体验优化

- **状态监控页分区整理**：
  - 状态监控页改为分区切换：运行概览、实时翻译、校对详情、错误诊断。
  - 保留原有统计卡片、进度条、实时翻译、译后校对详情和错误详情，但降低单页信息密度。
- **输入 EPUB 字符预估后台化**：
  - 输入或拖入 EPUB 后，字符预估改为后台线程执行，避免大文件读取时卡住 UI。
  - 增加短延迟防抖，减少输入变化时重复读取 EPUB。
- **暂停/恢复提示增强**：
  - 任务页操作区新增暂停说明。
  - 开始、暂停、已暂停、完成、失败时会更新提示，明确说明当前请求结束后停止、缓存保留、可切换模型后恢复。
- **深色主题修复**：
  - 修复术语表在深色主题下一行黑一行白的问题。
  - 为表格补充深色/浅色主题下的背景色、交替行、文字、选中行、网格线和表头样式。
  - 修复 API Key 测试结果弹窗在深色主题下只部分应用深色样式的问题，弹窗背景、文字、按钮和 Windows 标题栏会尽量跟随深色主题。
- **QML/V4 实验版隔离**：
  - 将 PySide6 + QML V4 原型移动到 `experimental/qml_v4/`。
  - `main_qt.py` + `ui/qt_app.py` 继续作为 V3.2.1 稳定主线。
  - 主线依赖 `requirements.txt` 恢复为 PyQt5/qfluentwidgets 方向。
  - QML/V4 实验版使用独立依赖文件 `experimental/qml_v4/requirements.txt`。
  - 修复实验版 `main.py` 初始化顺序，先创建 `QApplication` 再创建 QML bridge。
  - 修复术语页 `gbridge` 传参，避免页面切换后访问不到术语桥接器。
  - 修复译后校对字段映射，兼容 `draft/revised` 与旧的 `before/after` 字段名。
  - 修复术语表保存/导出丢失 `info/source` 字段的问题，并清理实验版 bridge 文件 BOM。
  - 调整实验版左侧导航顺序为：任务、状态、API、术语表、设置，并为当前页面按钮绑定激活状态。
  - 调整实验版“翻译设置”页面为可滚动布局，预设按钮和底部“界面与推理”选项支持换行，避免小窗口下底部设置被裁切。
  - 修复实验版术语表无法稳定选中单条术语的问题，新增“选择”列并改为新数组刷新选中状态，避免整行点击遮挡单元格编辑。
  - 接入实验版 `assets/logo.png`，用于 QML/V4 窗口图标，并在实验版 PyInstaller spec 中包含 assets 目录。
  - 修复实验版 PyInstaller spec 中 `__file__` 不可用的问题，使用 `SPECPATH` 定位实验目录，并打包生成 `dist/EPUB日译中V4.0.exe`。
  - 修复实验版状态页耗时显示逻辑，改为手动计时并在完成/失败/取消时保留最终耗时，避免时间显示异常或回退为 `--:--`。
  - 修复实验版校对详情页显示不完整的问题，改为列表式展示最近 300 条校对记录，显示可疑原因、日文残留/术语不一致触发状态、原文、初译和校对后译文，并自动滚动到最新记录。
  - 修复纯标点术语（如日文引号）误触发译后校对的问题，术语一致性校对只检查包含日文/中文/字母/数字的有效术语；校对详情会显示“校对结果：有变化/无变化”。
  - 增加译后校对兜底：如果因为日文残留触发校对，但校对后仍残留日文假名，则自动回退到单条重译，并在校对详情中标记“已回退单条重译”。
  - 修复实验版任务页拖入/选择新 EPUB 后输出路径仍显示上一次文件名的问题，新输入文件会自动刷新为当前书名对应的 `_zh.epub` 输出路径。
  - 调整实验版状态监控的启动状态文案，将 `开始翻译 文本块...` 改为 `开始翻译 预计翻译时长...`，按服务商、并发、批量、文本块和总字符数做启动阶段估算。
  - 优化实验版整体视觉风格：新增 `AppPalette.qml` 统一纸张背景、深青导航、琥珀强调色、卡片圆角和深浅色变量。
  - 重做实验版主框架左侧导航，改为纵向工作台导航，保持“任务、状态、API、术语表、设置”顺序，并增强当前页面激活态。
  - 重做实验版任务页和状态页的卡片布局：任务页突出拖拽导入、路径确认、预估字符和断点续译说明；状态页改为顶部进度面板、统计卡片、实时翻译、校对详情和错误诊断分区。
  - 将实验版状态页“校对详情”改为时间线式质检报告，逐条展示触发原因、日文残留/术语不一致标签、校对结果、原文、初译和校对后译文，并为新增记录增加轻微滑入动效。
  - 优化实验版术语表页面：新增总术语、自动提取、手动添加、未知来源统计卡；强化搜索筛选栏；加大表格行高；增强选中行背景、边框和勾选状态。
  - 调整实验版字体策略：为避免中文衬线/可变字体在 Qt/QML 下出现笔画发虚或裁切，正文和标题都优先使用稳定无衬线字体，顺序为 `HarmonyOS Sans SC`、`Microsoft YaHei UI`、`Microsoft YaHei`、`Noto Sans SC`。
  - 优化实验版任务页文字裁切：右上角版本胶囊加宽；源/输出文件路径改为中间省略显示；路径输入框默认显示开头，减少长路径只露出末尾造成的误判。
  - 修复实验版状态页“实时翻译”文字看起来不在框内的问题，改为外层卡片 + 内部文本框的固定结构，统一边距、背景、边框和裁剪。
  - 修复实验版深色主题未完整应用的问题：`AppPalette` 不再依赖 QML singleton 内部的 `Material.theme`，改为由主窗口把 `cfg.theme === "dark"` 显式绑定到全局调色板，确保背景、卡片、文字、边框和 Material 控件同步切换。
  - 统一实验版 API、术语表、设置页的基础主题背景和标题样式，减少页面切换时的视觉割裂。
  - 实验版入口和 spec 已改为子目录自洽，但仍需交互验证和补充 QML 测试覆盖后才能作为正式主线。

#### 术语表增强

- **术语来源显示优化**：
  - 术语表加载时，没有 `source` 字段的旧术语显示为“未知来源”。
  - 自动提取术语显示为“自动提取”，手动术语显示为“手动添加”。
  - 保存时会正确解析这些中文来源标签并写回 JSON，不会把来源误存到备注。
- **术语表搜索与筛选**：
  - 新增搜索框，支持按原文、译文、备注搜索。
  - 新增分类筛选：全部分类、Person、Location、Org、Item、Skill、Creature。
  - 新增来源筛选：全部来源、自动提取、手动添加、未知来源。
  - 筛选状态下保存不会丢失未显示的术语，保存前会合并当前可见编辑到完整术语缓存。
- **术语表管理操作**：
  - 新增“新增术语”“删除选中”“导出/备份 JSON”“恢复备份”按钮。
  - 新增/删除只在“启用术语表”后可操作。
  - 导出会按当前完整术语表生成 JSON 备份。
  - 恢复备份会先备份当前术语表，再用选择的 JSON 替换当前术语表。
- **术语来源数据链路修复**：
  - `normalize_glossary_payload()` 保留 `source` 字段，避免导入、归一化或保存后丢失来源。
  - 补充测试覆盖扁平对象格式和分类数组格式中的 `source=manual/auto` 保留逻辑。

#### 性能参数设置优化

- **Slider + SpinBox 联动**：
  - 设置页性能参数支持滑块和数字框双向联动。
  - 并发数、批量大小、批量字符上限、单条字符上限、超时都可以直接输入精确数字。
- **服务商性能预设**：
  - 新增预设按钮：智谱免费版、Gemini 免费版、DeepSeek 付费版、自定义。
  - 智谱免费版：并发 `1`、批量 `2`、批量字符 `200`、单条字符 `200`、超时 `300`。
  - Gemini 免费版：并发 `1`、批量 `2`、批量字符 `200`、单条字符 `200`、超时 `300`。
  - DeepSeek 付费版：并发 `12`、批量 `10`、批量字符 `4000`、单条字符 `1000`、超时 `120`。
- **预设激活状态**：
  - 当前选中的性能预设按钮会保持按下状态。
  - 手动修改任意性能参数后，会自动切回“自定义”按下状态。
- **切换服务商不再强制覆盖参数**：
  - 切换 GLM/Gemini/DeepSeek 时只显示推荐参数提示，不再自动改写用户当前设置。
  - 用户需要点击对应预设按钮才会应用推荐参数。

#### API 与译后校对

- **API 服务商能力提示**：
  - API 配置页根据当前服务商显示能力提示。
  - Gemini 提示不支持 DeepSeek 的 `thinking` 参数。
  - GLM/智谱提示免费版限流明显，建议使用低并发预设。
  - Sakura 提示本地服务不需要 API Key。
- **译后校对详情面板**：
  - 状态监控页新增“校对详情”分区。
  - 译后校对发现可疑译文时，UI 会显示可疑原因、原文、初译、校对后译文。
  - 同时显示是否由日文残留触发、是否由术语不一致触发。
  - 新任务会清空旧校对详情；暂停后恢复不会清空，保留暂停前记录。
- **校对详情回调链路**：
  - `translate_batch()` 新增 `proofread_callback`。
  - `TranslateWorker` 新增 `proofread_detail` 信号，把校对详情实时传到 Qt UI。
  - 补充测试验证校对回调包含原文、初译、校对后译文和日文残留标记。

#### 验证

- `python -m py_compile ui/qt_app.py translator.py glossary_store.py tests/test_translation_pipeline.py` 通过。
- `python -m pytest -q tests/test_translation_pipeline.py` 通过，当前回归：`21 passed`。
- Qt 主窗口实例化、状态页分区、术语表筛选、性能预设激活状态、深色主题表格与 API 测试弹窗样式均已做轻量验证。

### V3.2betaV1版（2026-05-26）

#### Qt UI 与交互修复

- **字体适配继续优化**：
  - UI 字号缩放固定为 `max(9, round(base_pt * 0.9))`，改善台式机 100% 缩放下字体偏小的问题。
  - 下拉框及下拉列表字号与整体 UI 字号保持一致。
  - 修复 `qfluentwidgets.ComboBox` 无 `view()` 方法导致的启动崩溃。
- **状态监控优化**：
  - 状态监控卡片中的“耗时”改为“预计剩余”，根据当前完成比例动态估算剩余时间。
  - 进度详情仍保留实时翻译字数、总字数和已耗时。
- **性能参数范围调整**：
  - 普通 Provider 参数上限调整为：并发 `25`、批量 `15`、单条字符 `1000`。
  - GLM 仍启用限流保护：并发≤2、批量≤2、批量字符≤500、单条字符≤150，并在 UI 中显示提示。

#### 术语表页面增强（2026-06-01）

- **左侧新增“术语表”页面**：
  - 独立展示当前 `glossary.json`，表格列包含分类、原文、译文、备注/来源。
  - 支持刷新、增量导入 JSON，导入时自动跳过重复术语并统计冲突。
- **术语表导入与展示不卡 UI**：
  - 术语表导入改为后台线程执行，导入期间按钮显示“正在导入...”并禁止重复点击。
  - 切换到术语表页面时，术语读取改为后台加载，表格按批次渲染，避免大术语表导致 Qt 界面无响应。
- **术语设置迁移与去重**：
  - “启用术语表”“自动提取术语（实验）”从设置页移到术语表页。
  - 设置页不再保留重复的术语设置入口。
  - “启用术语表”默认关闭，只有用户勾选后本次翻译才会启用术语表。
  - 未启用术语表时，“自动提取术语（实验）”不可选。
- **术语表可编辑保存**：
  - 勾选“启用术语表”后，术语表内容可直接在表格中编辑。
  - 编辑后“保存修改”按钮启用，保存时写回 `glossary.json`。
  - 保存时继续执行归一化和查重，重复/无效术语会被过滤。

#### 翻译流程与缓存修复

- **暂停/恢复可靠性增强**：
  - 批量翻译完成一个批次后立即写入缓存，暂停时强制落盘。
  - 暂停时会尝试关闭当前 HTTP session，减少等待已提交请求自然结束的时间。
- **模型隔离缓存**：
  - 缓存 key 纳入 `provider/model`，切换大模型后会重新翻译，不再复用旧模型译文。
  - 同一模型下仍支持缓存命中和断点续译。
- **Gemini 兼容修复**：
  - Gemini 请求不再发送不支持的 `thinking` 字段，避免 HTTP 400 `Invalid JSON payload`。
- **批量 JSON 温度统一**：
  - 所有 Provider 的 JSON 批量翻译 `temperature` 统一调整为 `0.1`。
  - 单条翻译温度保持原逻辑不变：Sakura 默认 `0.1`，其它 Provider 默认 `0.3`。
  - 该调整用于降低 JSON 格式错误、idx 缺失、条数不一致等批量解析风险。
- **译后校对（实验）**：
  - 设置页新增“启用译后校对”开关，默认关闭，避免默认增加 token 成本。
  - 开启后不做全量润色，只检查可疑译文：日文假名残留、术语表命中但译名缺失、空译文/过短/重复刷屏等。
  - 对可疑项调用 AI 校对修正，提示词限制为“不新增剧情、不删除信息、不改变原意、保持段落结构”。
  - 译后校对统计新增 `proofread_suspicious`、`proofread_fixed`。
- **目录页单链接残留修复**：
  - 修复 EPUB 正文目录中 `<a>中文标题</a>　日文标题` 这类单链接段落只替换链接文字、未删除链接外尾部日文的问题。
  - 修复后会保留 `<a href=...>` 链接，并清理同一段落内链接外的旧文本。

#### 打包与验证

- **重新打包 Qt 主线程序**：
  - 输出文件：`dist\EPUB日译中V3.2betaV1.exe`。
  - 本轮修复后已验证 Qt 主窗口可正常实例化。
- **回归验证**：
  - 翻译流水线测试覆盖模型隔离缓存与 Gemini `thinking` 字段回归。
  - 术语表页面、异步加载、可编辑保存、批量 JSON 温度、译后校对和目录残留修复后，当前回归：`21 passed`。

### V3.2beta（2026-05-25）

#### Qt 主线维护与稳定性优化

- **Qt 入口解耦 legacy Tk**：
  - 新增 `logging_config.py`，统一提供日志初始化。
  - `main_qt.py` 不再从 `app.py` 导入 `setup_logging`，降低 Qt 打包时被 Tk 旧入口和 `tkinterdnd2` 牵连的风险。
- **移除全局日志副作用**：
  - 删除 `translator.py` 模块导入时的 `logging.basicConfig()`。
  - 日志配置改为由入口统一管理，避免覆盖调用方日志设置。
- **缓存写入可靠性增强**：
  - `_save_cache()` 改为调用 `_atomic_write_json()`，缓存文件也使用原子写入。
  - `cache_store.atomic_write_json()` 与 `JaZhTranslator._atomic_write_json()` 改为显式 `.tmp + os.replace()`，避免当前 Windows 环境下 `tempfile.mkstemp()` 阻塞。
- **Qt 文案与版本号清理**：
  - Qt 窗口标题和侧边栏标题更新为 `EPUB 日译中 V3.2beta版`。
  - 清理 Qt 主线关键页面中的用户可见乱码文案。
- **Qt 字体适配修复**：
  - UI 字号缩放从 `base_pt * 0.7` 调整为 `base_pt * 0.9`。
  - 最小字号从 `7pt` 提升到 `9pt`，改善台式机 100% 缩放下字体过小的问题。
- **测试稳定性修复**：
  - 测试临时目录改为仓库内 `tests/_tmp`，不再依赖系统临时目录。
  - 当前回归：`15 passed`。

### V3.2beta（2026-05-22）

#### Qt 任务页暂停/恢复（断点续译）

- **新增“恢复”按钮**（任务页 -> 操作卡片）：
  - 翻译暂停后可直接点击“恢复”继续任务。
  - 恢复前可切换服务提供方、模型、API 参数。
- **暂停后保留已翻译进度**：
  - 依赖缓存命中实现断点续译，已翻译内容不会重复请求。
  - 恢复时会从未完成内容继续翻译。
- **取消分支强化缓存落盘**：
  - `TranslateWorker` 在取消路径会先 `flush_cache()`，再结束任务。
  - 避免暂停时已完成结果未写入缓存，提升可恢复性。
- **恢复安全校验**：
  - 恢复时要求输入/输出 EPUB 与暂停前一致，避免误恢复到其他任务。

#### Qt 文案乱码修复（本轮改动范围）

- 修复本轮新增流程中的 `??` 文案问题，包含：
  - “恢复”按钮文本
  - 暂停/恢复状态提示
  - 421/429 限流提示
  - 翻译失败弹窗标题与内容
- 修复“暂停后恢复按钮不可点击”的状态判断问题：
  - 根因是取消完成标记值在不同路径不一致（且受乱码影响）。
  - 统一使用哨兵值 `__CANCELLED__` 作为取消完成信号。
  - `TranslateWorker` 取消分支与 `_on_finished()` 判断口径一致，暂停后会正确启用“恢复”按钮。

### V3.2beta（2026-05-21）

#### 翻译核心增强（P0 级）

- **ID 强校验 + 部分重试（P0-A）**：
  - 批量 JSON 翻译结果校验从"全有全无"改为"部分成功保留 + 缺失索引重试"。
  - 模型返回部分有效 idx 时，有效条目直接使用，仅缺失条目走单条 `_translate_chunk` 兜底。
  - 越界/幻觉 idx 自动跳过，不再导致整批失败。
  - 新增统计项：`batch_json_partial_success`、`batch_partial_retry`。

- **finish_reason=length 截断续取（P0-B）**：
  - 新增 `MAX_CONTINUATIONS = 2`，当 API 返回 `finish_reason="length"` 时自动续取拼接。
  - 单条翻译（`_call_deepseek_single`）和批量 JSON 均支持截断续取。
  - 新增统计项：`truncation_continuation`。
  - 续取失败时优雅降级，返回已累积内容，不中断全书流程。

- **结果数据类重构**：
  - 新增 `SingleChunkResult` / `BatchJsonResult` 数据类，携带 `finish_reason`、`is_truncated` 等元信息。
  - 保留 `_call_deepseek()` 包装器向后兼容，返回字符串。

#### 质量增强

- **批量结果轻量质检**：
  - 新增 `repair_batch_quality()`，对可疑译文（空译文、过短译文、重复刷屏）单条重译。
  - 正常样本保持原批量结果，吞吐基本不受影响。
  - 质检触发时打印日志：`质检发现 N 条可疑译文，准备单条重译`。

#### Qt UI 改进

- **性能参数界面简化**：
  - 移除"性能预设"下拉框，改为直接展示滑块参数。
  - 参数标签中文化：`并发数`、`批量大小`、`批量字符上限`、`单条字符上限`、`超时(秒)`。
  - 默认值使用 balanced 预设（`max_workers=12, batch_size=10`）。

- **状态监控增强**：
  - 进度条右侧新增百分比文字显示（`0%` → `87%` → `100%`）。
  - 翻译完成时，若 P0 调试统计有数据，自动追加到"错误详情"面板展示。

- **导入修复**：
  - 补充 `Slider` 控件导入，修复启动报 `NameError`。

#### 项目结构整理

- **Tk UI 冻结标记**：
  - `app.py` 文件头部添加冻结说明（不再新增功能，仅接受阻断性 bug 修复）。
  - 主入口固定为 Qt UI（`main_qt.py`）。

- **旧版 spec 文件归档**：
  - 9 个 Tk 时代的 `.spec` 文件移入 `archived/` 目录。
  - 根目录仅保留 `main_qt.spec`。

### V3.1beta版（2026-05-19）

### Qt UI 与翻译参数（当日）

- **主题切换修复**：
  - 新增主题切换（`浅色 / 深色`）。
  - 修复主题切换后局部控件样式未刷新问题。
- **深度思考开关（Thinking Toggle）**：
  - Qt 设置项已打通 `TranslateConfig -> TranslateWorker -> JaZhTranslator` 参数链路。
  - 支持在 UI 中显式启用/禁用深度思考。
  - 默认对 `deepseek / doubao / gemini / custom` 注入 `thinking: {"type": "disabled"}` 以控制 token 成本并提升速度。
- **标题栏图标增强**：
  - 启动时按顺序探测并加载图标资源：
    1. `assets/logo.ico`
    2. `assets/logo.png`
    3. `logo.ico`
    4. `logo.png`
  - Windows 下补充任务栏图标 best-effort 设置（DWM API）。
- **字体与可读性修复**：
  - 优化 DPI 分档字体策略，避免高缩放下字体偏大。
  - 修复深色模式下部分文本对比度不足问题。
- **Qt 主线说明**：
  - Qt 版本继续作为当前主线迭代。
  - Tk 版本保持可用，但进入冻结维护（仅必要修复，不再扩展新特性）。
- **缓存与稳定性优化**：
  - 缓存文件写入改为原子写入，降低中断导致缓存损坏风险。
  - 引入缓存元信息（版本/Provider/Model），模型变更时自动失效旧缓存，避免质量回退。
- **续译引导文案增强**：
  - 421/429 场景提示补充“可直接重新开始，已翻译内容会命中缓存”。
- **EPUB 临时目录清理补强**：
  - 修复自动修复 EPUB 后再次读取失败时的临时目录泄漏。
- **文本切分优化（日文）**：
  - `_smart_split_text` 增加 `、` 作为可选切分点。


### V3.1beta（2026-05-18）

#### UI 字体适配优化（Qt UI）
- 全局字体统一缩小一档：
  - DPI 分档基准字号整体下调 1pt（`175%+/150%/125%/100%` 对应 `12/11/10/9pt`）
  - 基础控件统一字号从 `base+1` 调整为 `base`
- 在上述基础上，UI 字体再次统一缩小 20%：
  - 新增 `_ui_font_pt = max(7, round(base_pt * 0.8))`
  - 应用级字体与窗口字体统一使用 `_ui_font_pt`
  - `QLabel/QLineEdit/QPlainTextEdit/QComboBox/QPushButton/QCheckBox/QRadioButton` 统一使用 `_ui_font_pt`
- 保持布局不变：本次未调整任何控件间距/边距参数（spacing/margin 不变）
- 编译检查：`python -m py_compile ui/qt_app.py` 通过

### V3.1beta（2026-05-17）

#### 重构与交互优化（Qt UI）
- 左侧导航按钮补齐 `setCheckable(True)`，任务 / 接口配置 / 设置三按钮可正确进入选中态。
- 接口配置页联动增强：
  - 选择服务提供方后，`Base URL` 自动填充并锁定为不可编辑。
  - 仅 `Custom` 提供方允许手动编辑 `Base URL`。
  - 选择 `Sakura` 时，`API Key` 输入框自动置灰（离线本地模型无需手填 Key）。
- 任务页“开始翻译”旁按钮文案修复：乱码 `??` 已修正为 `暂停`。

#### 翻译流程与容错
- 修复 `TranslateWorker._extract_text()` 签名问题，消除：
  - `TypeError: TranslateWorker._extract_text() takes 1 positional argument but 2 were given`
- 修复 `TranslateWorker.cancel()` 误改逻辑，恢复为线程取消事件控制（`cancel_event.set()`）。
- 421/429 场景交互优化：
  - 触发限流/路由错误时给出明确提示。
  - 支持先暂停，再切换模型/提供方后继续翻译（基于现有缓存机制续跑）。

#### 状态监控增强
- 新增实时统计卡片：`Token 消耗`。
- `translator.py` 新增 `tokens_total` 统计字段。
- 在 API 响应中读取 `usage.total_tokens` 并累计，实时回传到状态页展示。
- UI 统计信号 `stat_update` 扩展 token 参数并在状态卡片实时刷新。

### v2.0.1（2026-05-15）

#### 新增功能
- **新增豆包（Doubao）服务提供方**：下拉选项新增 Doubao，支持火山引擎豆包大模型
- **API Key 连接测试**：接口配置页新增「测试连接」按钮，翻译前验证 Key 有效性，提前拦截 401/402 错误
- **拖拽上传 EPUB**：输入 EPUB 支持拖拽文件到输入框，自动识别路径并填入（需安装 tkinterdnd2）
- **术语表增量导入**：导入 JSON 时改为增量合并模式，不覆盖已有术语，冲突保留旧翻译

#### 参数调整
- **关闭深度思考**：DeepSeek/豆包请求自动注入 `thinking: {"type": "disabled"}`，降低 token 消耗
- **模型名更新**：
  - DeepSeek：`deepseek-chat` → `deepseek-v4-flash`
  - Doubao：`Doubao-Seed-1.6-flash`
  - Gemini：`gemini-2.5-pro` → `gemini-2.5-flash`

#### 稳定性修复
- **tkinterdnd2 可选依赖**：未安装时自动回退普通 Tk，应用不会启动崩溃
- **拖拽路径解析稳健性**：使用正则提取路径，正确处理空格路径和多文件场景
- **切换 Provider 不清空 Key**：误触下拉切换不再丢失已输入 API Key
- **备份提示条件准确**：导入术语表时提示与实际备份行为一致
- **内置模板自动释放**：打包 exe 后首次运行自动生成 `~/.epub_translator/dict/` 下 YAML 模板
  - 通过 `sys.frozen` 检测 PyInstaller 环境，打包模式下始终使用用户数据目录
  - 应用启动时主动调用 `get_dict_dir()` 预释放模板，无需等到翻译器初始化
  - 内置模板为代码内嵌副本，仅在目标文件不存在时写入，不覆盖用户自定义
- **术语表提示路径动态化**：提示文案使用 `get_data_dir()` 获取真实路径，不再硬编码用户名

#### 依赖更新
- `requirements.txt` 新增 `tkinterdnd2`

### v2.0（2026-05-14）

- UI 重构：左侧导航 + 右侧多页面布局，卡片化组件，主题配色系统
- 接口配置改为下拉框选择
- 状态监控页：8 项统计卡片 + 实时翻译展示（日文/中文逐条显示）
- 翻译详细状态改为日志输出，UI 保持简洁
- `translate_batch` 新增 `item_callback` 逐条回调
- 支持 PyInstaller 打包为 `EPUB日译中V2.0.exe`

### v1.5_b（2026-05-13 ~ 2026-05-14）

- 术语表数据链路重构：
  - 新增统一分类 schema：`Person/Location/Org/Item/Skill/Creature`
  - 新增导入归一化解析，兼容三种输入格式：
    - 扁平 `{src: dst}`
    - 扁平 `{src: {"dst": "...", "info": "..."}}`
    - 分类结构 `{Person:[...], ...}`
  - 导入冲突策略固定为 `keep_old`（保留旧值），并输出 `新增/跳过/冲突` 统计
- 术语文件可靠性增强：
  - 术语写入改为原子写入（`.tmp + replace`）
  - 导入备份改为时间戳版本：`glossary.backup.before_import.YYYYMMDD-HHMMSS.json`
- 术语召回优化（降低 token 与延迟）：
  - 新增“按当前文本批次召回术语”（精确匹配 + 去重 + 上限控制）
  - Prompt 注入从“全量术语”改为“仅注入命中术语”
- 自动提取术语并轨：
  - `_merge_new_terms_into_glossary()` 统一走同一 schema 与同一冲突策略（`keep_old`）
  - 自动提取术语保留 `info/source`（默认 `source="auto"`）用于审计
- 测试补齐与回归：
  - 新增导入归一化、冲突策略、原子写入、术语召回、自动术语并轨相关测试
  - 当前本地回归结果：`15 passed`

### v1.5_a

- **性能预设功能**：三种模式（默认/适中/极端），一键优化并发与批量参数
- **代码审查修复**：修复未定义变量、文件编码、异常处理、线程安全、响应验证等问题
- **连接池优化**：消除 "Connection pool is full" 警告
- **UI 改进**：翻译完成状态显示优化

### v1.5

- 多后端支持与模型/URL 可配置
- 字符预估、术语导入、实验性术语提取
- 批量 JSON 结构化翻译与容错增强
- 统计与日志增强
- 502 失败快速中断

### v1.4

- 实时统计面板
- 翻译耗时格式化显示

### v1.3

- GUI 线程安全修复
- EPUB 打包兼容性修复
- 加载异常处理增强

### v1.2

- 智能语言检测与文本分块
- 结构化批量翻译（早期版本）
- 连接复用与批量去重优化

### v1.1

- 目录翻译、超链接保留、Ruby 处理、图片占位过滤、翻页方向设置

### v1.0

- 基础翻译功能、缓存机制、术语表支持、GUI

## License

MIT License

## 启动入口（当前）

```bash
python main_qt.py
```

说明：`main_qt.py` 为当前主线入口。`python app.py` 为冻结维护的兼容入口。

## Qt 版打包（PyInstaller）

安装打包工具：

```bash
pip install pyinstaller
```

使用 spec 打包 Qt 入口：

```bash
pyinstaller main_qt.spec --noconfirm
```

输出文件：

```text
dist/EPUBTranslatorQt.exe
```

## Qt 回归清单（建议）

1. 启动与导航：`python main_qt.py`，检查 4 页面切换正常。  
2. 文件输入：按钮选择与拖拽 `.epub` 均可填充输入并生成输出路径。  
3. 接口配置：切换 provider 后 URL/Model 自动更新，测试连接可按超时返回结果。  
4. 翻译流程：开始后 UI 不冻结，进度条与统计卡片持续更新，取消可生效。  
5. 状态监控：实时原文/译文、错误详情、清空统计按钮可用。  
6. 术语功能：导入 JSON 增量合并成功，冲突与备份提示正确。  
7. 诊断包：导出 ZIP，包含脱敏配置、日志文件、glossary（若存在）。
